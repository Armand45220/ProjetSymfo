{% extends 'basebo.html.twig' %}

{% block body %}
    {% include "admin/_flashes.html.twig" %}
    <h1 class="title_bo">Ajouter un fichier à l'offre "{{ offre.nomOffre }}"</h1>
    <div class="ajoutOimgFlex"
        <div class="formAjoutOimg">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btnSub">Ajouter</button>
            {{ form_end(form) }}
        </div>
        <div class="back_path">
            <a href="{{ path('aff_img_admin', {id: offre.id}) }}">Retour</a>
        </div>
    </div>
{% endblock %}